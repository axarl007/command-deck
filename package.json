{
  "name": "command-deck-devtools",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling (icon generation, visual QA, Lighthouse). The app itself (index.html) has no build step and no dependencies.",
  "devDependencies": {
    "lighthouse": "^12.0.0",
    "playwright": "^1.56.1"
  }
}
